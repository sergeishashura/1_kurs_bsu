#include <vector>
using namespace std;
vector<vector<int>> matrixpower_for_matrix(vector<vector<int>>&, int size, int power);
vector<int> matrixpower_for_array(vector<int>& matrix_from_array, int size, int power);
